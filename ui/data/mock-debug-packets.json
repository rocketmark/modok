{
  "issue": {
    "summary": "Checkout fails after retrying payment with PaymentRetryError: max retries exceeded",
    "anchors": {
      "features": ["checkout", "payment-retry"],
      "errors": ["PaymentRetryError"],
      "symptoms": ["fails after retry", "max retries exceeded"]
    }
  },
  "affected_areas": [
    { "type": "feature", "id": "feature:checkout", "name": "checkout" },
    { "type": "feature", "id": "feature:payment-retry", "name": "payment-retry" }
  ],
  "relevant_files": [
    "src/checkout/retry.py",
    "src/payment/gateway.py",
    "src/checkout/session.py"
  ],
  "relevant_tests": [
    "tests/test_checkout_retry.py"
  ],
  "known_issues": [
    { "id": "known-issue:stagehand:checkout-retry-max", "summary": "Payment retry count not reset between checkout sessions" }
  ],
  "prior_fixes": [
    { "id": "fix:stagehand:reset-retry-counter", "commit": "abc1234", "summary": "Reset payment retry counter on session start" }
  ],
  "summary": "Checkout fails after retrying payment with PaymentRetryError: max retries exceeded"
}
